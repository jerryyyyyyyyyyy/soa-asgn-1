// Globals
/**
 * _driname = path to current directory
 * __filename = file name
 * require = function to use modules (CommonJS)
 * module = information about current module (file)
 * process = information about the enivronment where the program is being executed.
 *
 */
 console.log(__dirname);
 setInterval(()=>{
     console.log('SetInterval Function running and Hello World.');
 }, 1000)